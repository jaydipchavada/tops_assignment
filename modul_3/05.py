# How will you compare two lists?
# ==> we can compare two lists directly using the == operator. It checks if both lists have the same elements in the same order.

list1 = [1, 2, 3]
list2 = [1, 2, 3]
if list1 == list2:
    print("Both lists are equal")