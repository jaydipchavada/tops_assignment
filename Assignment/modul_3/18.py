"""
# What is tuple? Difference between list and tuple.

    -> tuple is a collection data type which is contain similar and dis-similar data elements.
    -> tuple is represent by () breces
    -> tuple is a immutable. or fixed collections of related values

    # diff between list and tuple
 
    ==> list is represent by [] breces or tuple is represent by () breces
 
    ==> list which is mutable so Lists have more built-in methods and operations compared to tuples. when tuple is a immutable 

    ==> list is slower then tuple 

    
"""